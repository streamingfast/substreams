name: Build and Test

on:
  pull_request:
  push:
    branches:
      - develop
      - feature/*

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    if: "!startsWith(github.event.head_commit.message, 'GitBook: [#')"
    strategy:
      matrix:
        go-version: [1.25.x] # if you change these values, you need to change the github PR compliance requirements
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Check out code
        uses: actions/checkout@v4

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          # In order:
          # * Module download cache
          # * Build cache (Linux)
          # * Build cache (Mac)
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
            ~/Library/Caches/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Validate GitBook
        run: go run ./docs/.gitbook/validate.go

      - name: Run Tests
        run: ./bin/test.sh

      - name: Build
        run: go build ./...
