name: Build and Test

on:
  pull_request:
  push:
    branches:
      - develop
      - feature/*

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    if: "!startsWith(github.event.head_commit.message, 'GitBook: [#')"
    strategy:
      matrix:
        go-version: [1.26.x] # if you change these values, you need to change the github PR compliance requirements
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Check out code
        uses: actions/checkout@v4

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          # In order:
          # * Module download cache
          # * Build cache (Linux)
          # * Build cache (Mac)
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
            ~/Library/Caches/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Validate GitBook
        run: go run ./docs/.gitbook/validate.go

      - name: Run Tests
        env:
          # Docker-backed sink/sql integration tests, only ubuntu runners have Docker
          SF_SINK_SQL_INTEGRATION_TESTS: ${{ matrix.os == 'ubuntu-latest' && 'true' || '' }}
        run: ./bin/test.sh

      - name: Build
        run: go build ./...
