// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.0
// - protoc             (unknown)
// source: sf/substreams/rpc/v4/estimate.proto

package pbsubstreamsrpcv4

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	Estimator_Estimate_FullMethodName = "/sf.substreams.rpc.v4.Estimator/Estimate"
)

// EstimatorClient is the client API for Estimator service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// Estimator computes what running a Substreams request would cost, without ever sending
// the module data back to the client.
//
// The two cost drivers of a real request are (A) the number of blocks that must be
// processed and (B) the volume of uncompressed data sent back. (A) is derived from the
// module graph and from what is already in the cache. (B) is measured: a small,
// configurable fraction of the requested range is actually executed on tier2 workers, and
// only the *size* of the resulting output cache is read back, then extrapolated over the
// full range.
type EstimatorClient interface {
	// Estimate samples the requested range and returns a cost estimate. The stream emits
	// `EstimateProgress` messages while the sample jobs run, then exactly one `Estimate`
	// message before completing. No module output is ever sent.
	Estimate(ctx context.Context, in *EstimateRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[EstimateResponse], error)
}

type estimatorClient struct {
	cc grpc.ClientConnInterface
}

func NewEstimatorClient(cc grpc.ClientConnInterface) EstimatorClient {
	return &estimatorClient{cc}
}

func (c *estimatorClient) Estimate(ctx context.Context, in *EstimateRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[EstimateResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &Estimator_ServiceDesc.Streams[0], Estimator_Estimate_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[EstimateRequest, EstimateResponse]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type Estimator_EstimateClient = grpc.ServerStreamingClient[EstimateResponse]

// EstimatorServer is the server API for Estimator service.
// All implementations should embed UnimplementedEstimatorServer
// for forward compatibility.
//
// Estimator computes what running a Substreams request would cost, without ever sending
// the module data back to the client.
//
// The two cost drivers of a real request are (A) the number of blocks that must be
// processed and (B) the volume of uncompressed data sent back. (A) is derived from the
// module graph and from what is already in the cache. (B) is measured: a small,
// configurable fraction of the requested range is actually executed on tier2 workers, and
// only the *size* of the resulting output cache is read back, then extrapolated over the
// full range.
type EstimatorServer interface {
	// Estimate samples the requested range and returns a cost estimate. The stream emits
	// `EstimateProgress` messages while the sample jobs run, then exactly one `Estimate`
	// message before completing. No module output is ever sent.
	Estimate(*EstimateRequest, grpc.ServerStreamingServer[EstimateResponse]) error
}

// UnimplementedEstimatorServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedEstimatorServer struct{}

func (UnimplementedEstimatorServer) Estimate(*EstimateRequest, grpc.ServerStreamingServer[EstimateResponse]) error {
	return status.Error(codes.Unimplemented, "method Estimate not implemented")
}
func (UnimplementedEstimatorServer) testEmbeddedByValue() {}

// UnsafeEstimatorServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to EstimatorServer will
// result in compilation errors.
type UnsafeEstimatorServer interface {
	mustEmbedUnimplementedEstimatorServer()
}

func RegisterEstimatorServer(s grpc.ServiceRegistrar, srv EstimatorServer) {
	// If the following call panics, it indicates UnimplementedEstimatorServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Estimator_ServiceDesc, srv)
}

func _Estimator_Estimate_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(EstimateRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(EstimatorServer).Estimate(m, &grpc.GenericServerStream[EstimateRequest, EstimateResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type Estimator_EstimateServer = grpc.ServerStreamingServer[EstimateResponse]

// Estimator_ServiceDesc is the grpc.ServiceDesc for Estimator service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Estimator_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "sf.substreams.rpc.v4.Estimator",
	HandlerType: (*EstimatorServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Estimate",
			Handler:       _Estimator_Estimate_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "sf/substreams/rpc/v4/estimate.proto",
}
