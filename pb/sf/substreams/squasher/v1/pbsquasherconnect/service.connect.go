// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: sf/substreams/squasher/v1/service.proto

package pbsquasherconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/streamingfast/substreams/pb/sf/substreams/squasher/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SquasherName is the fully-qualified name of the Squasher service.
	SquasherName = "sf.substreams.squasher.v1.Squasher"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SquasherSquashProcedure is the fully-qualified name of the Squasher's Squash RPC.
	SquasherSquashProcedure = "/sf.substreams.squasher.v1.Squasher/Squash"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	squasherServiceDescriptor      = v1.File_sf_substreams_squasher_v1_service_proto.Services().ByName("Squasher")
	squasherSquashMethodDescriptor = squasherServiceDescriptor.Methods().ByName("Squash")
)

// SquasherClient is a client for the sf.substreams.squasher.v1.Squasher service.
type SquasherClient interface {
	// Squash covers every range in order for one store module. The server keeps
	// the full KV hot, copies over empty partials, and merges the rest. The RPC
	// returns after the last full KV is on disk.
	Squash(context.Context, *connect.Request[v1.SquashRequest]) (*connect.Response[v1.SquashResponse], error)
}

// NewSquasherClient constructs a client for the sf.substreams.squasher.v1.Squasher service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSquasherClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SquasherClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &squasherClient{
		squash: connect.NewClient[v1.SquashRequest, v1.SquashResponse](
			httpClient,
			baseURL+SquasherSquashProcedure,
			connect.WithSchema(squasherSquashMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// squasherClient implements SquasherClient.
type squasherClient struct {
	squash *connect.Client[v1.SquashRequest, v1.SquashResponse]
}

// Squash calls sf.substreams.squasher.v1.Squasher.Squash.
func (c *squasherClient) Squash(ctx context.Context, req *connect.Request[v1.SquashRequest]) (*connect.Response[v1.SquashResponse], error) {
	return c.squash.CallUnary(ctx, req)
}

// SquasherHandler is an implementation of the sf.substreams.squasher.v1.Squasher service.
type SquasherHandler interface {
	// Squash covers every range in order for one store module. The server keeps
	// the full KV hot, copies over empty partials, and merges the rest. The RPC
	// returns after the last full KV is on disk.
	Squash(context.Context, *connect.Request[v1.SquashRequest]) (*connect.Response[v1.SquashResponse], error)
}

// NewSquasherHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSquasherHandler(svc SquasherHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	squasherSquashHandler := connect.NewUnaryHandler(
		SquasherSquashProcedure,
		svc.Squash,
		connect.WithSchema(squasherSquashMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/sf.substreams.squasher.v1.Squasher/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SquasherSquashProcedure:
			squasherSquashHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSquasherHandler returns CodeUnimplemented from all methods.
type UnimplementedSquasherHandler struct{}

func (UnimplementedSquasherHandler) Squash(context.Context, *connect.Request[v1.SquashRequest]) (*connect.Response[v1.SquashResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.substreams.squasher.v1.Squasher.Squash is not implemented"))
}
