version: v1
name: buf.build/streamingfast/substreams

deps: ["buf.build/streamingfast/firehose"]

build:
  excludes:
  - google
  - sf/substreams/v1/test

lint:
  use:
    - DEFAULT

breaking:
  use:
    - FILE
